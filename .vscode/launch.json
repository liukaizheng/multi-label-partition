{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug multi-label-partition",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/multi-label-partition",
            "args": ["-m", "cube_corner_stamped.off", "-t", "cube_corner_stamped.off_.msh"],
            "cwd": "${workspaceFolder}/build",
            "env": {},
            "stopOnEntry": false,
            "preLaunchTask": "Build multi-label-partition",
            "console": "integratedTerminal"
        }
    ]
}
