{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug relief_app",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/multi-label-partition",
            "args": ["-m", "cube_stamped.off", "-t", "cube_stamped.off_.msh"],
            "cwd": "${workspaceFolder}/build",
            "env": {},
            "stopOnEntry": false,
            "preLaunchTask": "Build relief_app",
            "console": "integratedTerminal"
        }
    ]
}
